export const config = Object.freeze({
    // commands: ['!hitsquad', '!battleroyale', '!gauntlet'],
    intervalBetweenRounds: 5 * 60 * 1000,
    // intervalBetweenRounds: 20 * 1000,
    intervalBetweenCommands: 5000
});
