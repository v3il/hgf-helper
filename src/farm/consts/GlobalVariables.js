import { Timing } from './Timing';

export const GlobalVariables = Object.freeze({
    ANTI_CHEAT_DURATION: 2 * Timing.MINUTE,
    HITSQUAD_GAMES_ON_SCREEN: 12,
    PAGE_RELOAD_ROUNDS: 10,
    DECREASE_DELAY_ROUNDS: 20
});
